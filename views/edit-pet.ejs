<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit <%= pet.name %> | Pet Adoption</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Source+Sans+3:wght@400;600;700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/styles.css" />
</head>

<body>
  <div class="page-section">
    <div class="page-section-inner">
      <a href="/admin" class="small-link">&laquo; Back to admin dashboard</a>
      <h1 class="page-section-title mb-big">Update a Pet</h1>
      <form id="manage-pet-form" action="/admin/edit-pet" method="POST">
        <input type="hidden" name="_id" value="<%= pet._id %>" />
        <input autofocus autocomplete="off" class="form-field" type="text" name="name" value="<%= pet.name %>" />
        <input autocomplete="off" class="form-field" type="text" name="birthYear" value="<%= pet.birthYear %>" />
        <select class="form-field" id="species-select" name="species">
          <option>dog</option>
          <option>cat</option>
        </select>
        <textarea class="form-field" name="description"><%= pet.description %></textarea>

        <input class="form-field" id="file-field" type="file" />

        <div class="photo-preview">
          <img src="<%= pet.photo %>" />
        </div>

        <input id="public_id" type="hidden" name="public_id" />
        <input id="version" type="hidden" name="version" />
        <input id="signature" type="hidden" name="signature" />

        <button id="submit-btn" class="our-btn">Submit Changes</button>
      </form>
    </div>
  </div>

  <footer class="site-footer">
    <p>&copy; 2023 Fake Adoption Center. All rights reserved.</p>
  </footer>

  <script>
    const species = "<%= pet.species %>"
    document.getElementById("species-select").value = species
  </script>

  <script>
    const api_key = "<%= apikey %>"
    const cloud_name = "<%= cloudname %>"
  </script>

  <script src="https://cdn.jsdelivr.net/npm/axios@1.1.2/dist/axios.min.js"></script>
  <script src="/js/add-pet.js"></script>
</body>

</html>